---
const {color, tittle, id, subtittle, showFooterContent} = Astro.props
const textColor = `text-${color}`
import FooterMenu from './FooterMenu.astro';
---

<section
  class="landing-section bg-black h-screen w-screen text-center overflow-hidden relative"
  data-header-color={color}
  id={id}
>
  <div class="z-30 relative h-full flex flex-col">
    <header>
      <h2 class={`${textColor} pt-20 text-[40px] font-medium`}>
        {tittle}
      </h2>
      <p class={`${textColor} text-4xl`}>
        {subtittle}
      </p>
    </header>
    <div class="flex items-center justify-center">
      <slot name="contenido"></slot>
    </div>
    <footer class="flex flex-grow justify-end flex-col pb-20">
      <div>
        <slot name="footer" />
        <!-- Contidionally load footer: -->
      {showFooterContent && <FooterMenu />}
      </div>
    </footer>
  </div>

  <div class="absolute top-0 bottom-0 h-full w-full z-10">
    <slot name="background"></slot>
  </div>
</section>
